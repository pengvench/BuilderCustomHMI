<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:CustomHMIBuilder.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view="clr-namespace:CustomHMIBuilder.Widgets.View"
        xmlns:viewModels="clr-namespace:CustomHMIBuilder.MainWindows.ViewModels"
        mc:Ignorable="d"
        x:Class="CustomHMIBuilder.MainWindows.Views.MainWindow"
        Icon="/Assets/avalonia-logo.ico"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        MinWidth="1200" MinHeight="720"
        Title="BuilderCustomHMI"
        x:DataType="viewModels:MainWindowViewModel">
    
    
    <!--Это основной Grid который задает кол-во строчек-->
    <Grid RowDefinitions="Auto,Auto,*,Auto">
        <!-- Хедер где будет какая-то кака-->

        <DockPanel Grid.Row="0">
            <Menu DockPanel.Dock="Left" Height="20">
                <MenuItem Header="File" FontSize="12">
                    <MenuItem Header="New" InputGesture="Ctrl+N" FontSize="12" />
                    <MenuItem Header="Open" InputGesture="Ctrl+O" FontSize="12" />
                    <Separator />
                    <MenuItem Header="Export"  FontSize="12" />
                    <Separator />
                    <MenuItem Header="Save" InputGesture="Ctrl+S" FontSize="12" />
                    <MenuItem Header="Save As" InputGesture="Ctrl+Shift+S" FontSize="12" />
                    <Separator />
                    <MenuItem Header="Exit" InputGesture="Alt+F4" FontSize="12" />
                </MenuItem>
                <MenuItem Header="Edit" FontSize="12">
                    <Separator />
                    <MenuItem Header="Copy" FontSize="12" />
                    <MenuItem Header="Paste" FontSize="12" />
                    <Separator />
                    <MenuItem Header="Cancel" FontSize="12" />
                </MenuItem>
                <MenuItem Header="Help" FontSize="12">
                    <MenuItem Header="About" FontSize="12" />
                </MenuItem>
            </Menu>
            <TextBlock />
        </DockPanel>

        <!-- Второй хедер, где находюца панели "Конструктор" и "Пульт"-->
        <Grid Grid.Row="1" Height="40" Background="LightSteelBlue" ColumnDefinitions="Auto,Auto,Auto,*,Auto">
            <!--панель левых кнопок-->
            <Button Grid.Column="0" Name="Home" Click="Button_OnClick" Content="Главная" Background="Transparent" FontWeight="Medium"
                    Margin="5" />
            <Button Grid.Column="1" Name="Editor" Click="Button_OnClick" Content="Кострутор"
                    Background="Transparent" FontWeight="Medium" Margin="5" />

            <Button Grid.Column="4" HorizontalAlignment="Right" Name="ConnectDevice" Click="ConnectDevice_OnClick" Content="Подключить устройство"
                    FontWeight="Medium" Margin="5" Background="LightSeaGreen" Foreground="White" />
        </Grid>

        <!-- Это короче основная область, где будет спавниться окно -->
        <Grid Grid.Row="2" Name="MainGrid" Background="#6d8392" >
            <view:HomeWidgetsView/>
        </Grid>
    </Grid>


</Window>